- name: copy and cat
  hosts: localhost
  connection: local
  tasks:
    - name: Create hello1.txt
      copy:
        content: "Hello from Jenkins + Ansible!"
        dest: /tmp/hello1.txt
    - name: Display hello1.txt contents
      command: cat /tmp/hello1.txt
      register: result

    - name: Show output
      debug:
        msg: "{{ result.stdout }}"
